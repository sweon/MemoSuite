{
    "name": "memosuite-monorepo",
    "private": true,
    "description": "Monorepo for Memo apps suite",
    "license": "GPL-3.0",
    "scripts": {
        "dev": "pnpm -r --parallel dev & vite",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint"
    },
    "devDependencies": {
        "vite": "^7.3.1"
    },
    "dependencies": {
        "pdfjs-dist": "4.10.38"
    },
    "pnpm": {
        "overrides": {
            "@types/react": "18.3.12"
        }
    }
}